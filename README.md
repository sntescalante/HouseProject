# HouseProject
Complete IoT system using Arduino, a backend API, and a frontend dashboard. The Arduino reads sensors and controls devices, the backend manages data and real-time communication, and the frontend displays live metrics and lets users interact with the system.
